// Per-lab Rust modules live here, one submodule per lab id (e.g. `svg`).
//
// Each lab's commands are prefixed with its id (e.g. `svg_import`) and listed
// explicitly in `lib.rs`'s `generate_handler!`, grouped by lab with a comment
// banner — see docs/plan.md §3.
//
// (c) Copyright 2026 Liminal HQ, Scott Morris
// SPDX-License-Identifier: MIT

pub mod svg;
